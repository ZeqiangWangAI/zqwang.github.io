I will update the personal homepage with the following steps:

1.  **Update Site Configuration (`_config.yml`)**:
    *   Set `title` and `author` to "Zeqiang Wang".
    *   Update `avatar` to use the provided image: `/Image/IMG_0037.PNG`.
    *   Update `description` to "Personal Homepage of Zeqiang Wang".
    *   Remove default email and social links.

2.  **Clean Up Example Content**:
    *   Delete the default Jekyll example posts in the `_posts/` directory.

3.  **Update Homepage (`index.html`)**:
    *   Replace the current post list layout with a personal introduction layout.
    *   Add a brief "About Me" section (using placeholder text as I cannot extract text from the PDF CVs).
    *   Add prominent download links for the CVs:
        *   English CV: `/CV/Zeqiang_Wang_University_of_Surrey_EN.pdf`
        *   Chinese CV: `/CV/Zeqiang_Wang_University_of_Surrey_CN.pdf`

4.  **Update Social Links (`_data/social.yml`)**:
    *   Remove the default "Shangzhi Huang" social links.

**Note**: Since I cannot read the content of the PDF files, I will use placeholder text for your bio/introduction. You can easily edit `index.html` later to add your specific details.